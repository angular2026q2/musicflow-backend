# musicflow-backend
Scalable backend for MusicFlow streaming platform. Implements REST API using NestJS (TypeScript), JWT authentication with refresh token rotation, and Supabase (PostgreSQL) for persistence. Integrates with Jamendo API for music data.
